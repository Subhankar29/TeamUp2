<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="app.first.in.collegeprofiles">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="TeamUp"

        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SignupScreen"
            android:label="Details"
            />
        <activity android:name=".Logged"
            />
        <activity android:name=".Field_Profile"
            android:label="Profile" />

        <activity android:name=".SignupScreen22"
            android:label="Interests"
            />
        <activity android:name=".Tennis_People" />
        <activity
            android:name=".MainActivity2"
            android:label="Teamup"
            android:theme="@style/AppTheme.NoActionBar"></activity>



    </application>

</manifest>